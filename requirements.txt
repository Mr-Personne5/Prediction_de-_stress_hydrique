# ============================================================
# requirements.txt — Master IASD — Stress Hydrique Haute Guinée
# Djiba Kaba — UKAG — Avril 2026
#
# Installation :
#   pip install -r requirements.txt
#
# IMPORTANT : installer PyTorch séparément d'abord (voir note bas)
# ============================================================

# --- Deep Learning ---
torch>=2.0.0
torchvision>=0.15.0

# --- Google Earth Engine ---
earthengine-api>=0.1.370
geemap>=0.30.0

# --- Manipulation données raster / géospatiales ---
rasterio>=1.3.0
xarray>=2023.1.0
netCDF4>=1.6.0
geopandas>=0.14.0
pyproj>=3.6.0
shapely>=2.0.0

# --- Calcul scientifique ---
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# --- Indices climatiques (SPEI, SPI, VHI) ---
climate-indices>=1.2.0

# --- Machine Learning baseline ---
scikit-learn>=1.3.0

# --- Visualisation ---
matplotlib>=3.7.0
seaborn>=0.12.0
cartopy>=0.22.0

# --- Suivi progression ---
tqdm>=4.65.0

# --- Notebooks ---
jupyter>=1.0.0
ipykernel>=6.0.0

# ============================================================
# NOTE — Installation PyTorch avec support CUDA (RTX 4060)
#
# NE PAS installer torch via ce fichier directement si tu veux
# le support GPU. Exécuter d'abord cette commande séparément :
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# Puis vérifier que le GPU est détecté :
#   python -c "import torch; print(torch.cuda.is_available())"
#   → doit afficher : True
#
# Ensuite seulement :
#   pip install -r requirements.txt
#
# Compatible : Python 3.12.x + PyTorch 2.7.x + CUDA 12.8
# ============================================================